#include "main.h"

/**
 * _abs - computes absolutes value of an integer
 * @n: number to be return
 * return: return its value
*/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
      	}
	return (-n);
}
