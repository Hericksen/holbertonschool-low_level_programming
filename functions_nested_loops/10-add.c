#include "main.h"

/**
 * add - add two integers and returns the result
 * @a: first integers
 * @b: second integers
 * Return: return the result of a + b
*/

int add(int a, int b)
{
	return (a + b);
}
